/* ═══════════════════════════════════════════════════════════
   HUD — стили интерфейса игрока
   ═══════════════════════════════════════════════════════════ */

.hud-root {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
}

.hud-top-pill-container {
    position: absolute;
    top: var(--space-4);
    left: 0;
    right: 0;
    align-items: center;
}

.hud-top-pill {
    flex-direction: row;
    align-items: center;
    padding: var(--space-1) var(--space-4);
    background-color: var(--surface-void-shade);
    border-color: var(--border-subtle);
    border-width: 1px;
    border-radius: var(--radius-md);
}

.hud-top-pill-version {
    -unity-font-definition: var(--face-data);
    font-size: var(--size-xs);
    color: var(--text-tertiary);
    margin: 0;
    padding: 0;
    -unity-text-align: middle-center;
}

.hud-top-pill-divider {
    width: 1px;
    height: var(--space-6);
    background-color: var(--border-line);
    margin-left: var(--space-3);
    margin-right: var(--space-3);
}

.hud-top-pill-fps {
    -unity-font-definition: var(--face-data);
    font-size: var(--size-xs);
    color: var(--light-solid);
    margin: 0;
    padding: 0;
    -unity-text-align: middle-center;
}

.hud-top-pill-asset {
    flex-direction: row;
    align-items: center;
}

.hud-top-pill-dot {
    width: var(--space-3);
    height: var(--space-3);
    border-radius: var(--radius-pill);
    background-color: var(--state-ok);
    margin-right: var(--space-2);
}

.hud-top-pill-dot.is-loading {
    background-color: var(--accent-gold);
}

.hud-top-pill-asset-text {
    -unity-font-definition: var(--face-data);
    font-size: var(--size-xs);
    color: var(--text-secondary);
    margin: 0;
    padding: 0;
    -unity-text-align: middle-center;
}

.hud-fps-counter {
    padding: var(--space-1) var(--space-4);
    background-color: var(--surface-void-shade);
    border-radius: var(--radius-md);
    color: var(--light-solid);
    font-size: var(--size-base);
    -unity-text-align: upper-center;
}

.world-map-overlay {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    justify-content: center;
    align-items: center;
    background-color: var(--surface-scrim);
}

.world-map-image {
    width: 100%;
    height: 100%;
    -unity-background-scale-mode: scale-to-fit;
}

.hud-top-left {
    position: absolute;
    top: var(--space-5);
    left: var(--space-5);
}

.hud-top-right {
    position: absolute;
    top: var(--space-5);
    right: var(--space-5);
}

.hud-bottom-left {
    position: absolute;
    bottom: var(--space-5);
    left: var(--space-5);
}

.hud-bottom-right {
    position: absolute;
    bottom: var(--space-5);
    right: var(--space-5);
}

.asset-status-root {
    position: absolute;
    right: var(--space-5);
    bottom: var(--space-5);
    align-items: flex-end;
}

.asset-status-button {
    min-width: 38px;
    width: auto;
    height: var(--space-11);
    padding: 0 var(--space-4);
    background-color: var(--surface-slate-dense);
    border-color: var(--text-tertiary);
    border-width: 1px;
    border-radius: var(--radius-md);
    color: var(--light-solid);
    -unity-text-align: middle-center;
}

.asset-status-button:hover,
.asset-status-button.asset-status-loading {
    background-color: var(--surface-shelf-solid);
    border-color: var(--accent-gold);
}

.asset-status-button-label {
    color: var(--light-solid);
    font-size: var(--size-sm);
    -unity-text-align: middle-center;
}

.asset-status-panel {
    width: 300px;
    margin-bottom: var(--space-3);
    padding: var(--space-4);
    background-color: var(--surface-solid);
    border-color: var(--accent-cyan-fill);
    border-width: 1px;
    border-radius: var(--radius-md);
}

.asset-status-summary,
.asset-status-metrics,
.asset-status-details {
    color: var(--light-solid);
    font-size: var(--size-sm);
    white-space: normal;
}

.asset-status-metrics {
    margin-top: 5px;
    color: var(--text-secondary);
}

.asset-status-details {
    margin-top: 5px;
    color: var(--text-secondary);
}

.hud-button {
    background-color: var(--surface-raised);
    border-color: var(--border-subtle);
    border-width: var(--space-1);
    border-radius: var(--radius-sm);
    padding: 0;
    min-width: 100px;
    height: var(--space-11);
    font-size: var(--size-sm);
    color: var(--text-primary);
    -unity-text-align: middle-center;
}

.hud-button:hover {
    background-color: var(--accent-cyan-wash);
}

.hud-button.active {
    background-color: var(--accent-gold);
    color: var(--shadow-solid);
}

.hud-stat {
    color: var(--text-primary);
    font-size: var(--size-md);
    margin: var(--space-1) 0;
}

.hud-stat-label {
    color: var(--text-secondary);
    font-size: var(--size-sm);
}

.hud-bar {
    background-color: var(--surface-shelf-solid);
    border-color: var(--border-subtle);
    border-width: 1px;
    border-radius: var(--radius-sharp);
    height: var(--space-6);
    width: 150px;
}

.hud-bar-fill {
    background-color: var(--accent-gold);
    border-radius: var(--radius-sharp);
}

.hud-panel {
    position: absolute;
    top: var(--space-5);
    left: var(--space-5);
    width: 240px;
    padding: var(--space-6);
    background-color: var(--surface-panel);
    border-color: var(--border-subtle);
    border-width: var(--space-1);
    flex-direction: column;
}

.hud-title-row {
    flex-direction: row;
    margin-bottom: var(--space-5);
    align-items: center;
}

.hud-clan-button {
    width: 15px;
    height: 15px;
    flex-shrink: 0;
    padding: 0;
    margin-left: var(--space-2);
    background-color: var(--accent-gold-glow);
    border-width: 1px;
    border-color: var(--text-disabled);
}

.hud-panel .hud-title-row {
    flex-direction: row;
    margin-bottom: var(--space-2);
}

.hud-panel .hud-nickname {
    font-size: var(--size-lg);
    -unity-font-style: bold;
    color: var(--accent-gold);
    flex-grow: 1;
}

.hud-panel .hud-level {
    font-size: var(--size-lg);
    color: var(--text-primary);
    margin-right: var(--space-1);
}

.hud-panel .hud-separator {
    height: 1px;
    background-color: var(--text-secondary);
    margin-bottom: var(--space-2);
}

.hud-panel .hud-stat {
    font-size: var(--size-md);
    color: var(--text-primary);
    margin: 0 0 var(--space-1);
}

.hud-panel .hud-money {
    -unity-font-style: bold;
    font-size: var(--size-md);
    color: var(--text-secondary);
    margin: 0;
}

.hud-panel .hud-creds {
    -unity-font-style: bold;
    font-size: var(--size-md);
    color: var(--accent-gold);
    margin: 0;
}

.hud-panel .hud-basket {
    -unity-font-style: bold;
    font-size: var(--size-md);
    color: var(--accent-gold);
    margin-bottom: var(--space-1);
}

.hud-basket-container {
    flex-direction: column;
}

.hud-stat-wrap {
    white-space: normal;
}

.hud-hp-header {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
}

.hud-hp-bar {
    height: var(--space-7);
    background-color: var(--surface-shelf-solid);
    border-radius: var(--radius-sm);
    flex-direction: row;
    margin-bottom: var(--space-2);
}

.hud-hp-fill {
    height: var(--space-7);
    border-radius: var(--radius-sm);
}

.hud-button-accent {
    background-color: var(--accent-gold);
    border-color: var(--border-subtle);
    border-width: var(--space-1);
    border-radius: var(--radius-sm);
    color: var(--surface-shelf-solid);
    font-size: var(--size-md);
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    padding: 0;
}

.hud-button-accent:hover {
    background-color: var(--accent-gold-hover);
}

.hud-button-close {
    width: var(--space-10);
    height: var(--space-10);
    background-color: transparent;
    color: var(--text-secondary);
    font-size: var(--size-lg);
    padding: 0;
    border-width: 0;
}

.hud-button-claim {
    align-self: center;
    background-color: var(--accent-cyan-glow);
    color: var(--light-solid);
    border-color: var(--light-edge);
    border-width: var(--space-1);
    border-radius: var(--radius-sm);
    padding: var(--space-2) var(--space-8);
    font-size: var(--size-md);
}

.hud-button-claim:hover {
    background-color: var(--light-edge);
}

.hud-button-label {
    font-size: var(--size-sm);
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    flex-grow: 1;
}

.hud-bonus-panel {
    position: absolute;
    top: var(--space-5);
    left: 256px;
    width: 260px;
    padding: var(--space-5);
    background-color: var(--surface-panel);
    border-color: var(--border-subtle);
    border-width: var(--space-1);
    flex-direction: column;
}

.hud-bonus-panel .hud-stat {
    margin: var(--space-1) 0;
}

.hud-bonus-panel .hud-title-row {
    flex-direction: row;
    margin-bottom: var(--space-5);
}

.hud-bonus-panel .hud-title-row .hud-stat {
    color: var(--accent-gold);
    flex-grow: 1;
}

.hud-status-panel {
    position: absolute;
    /* Ниже кнопки бонуса (top:10 + высота 50 + зазор 6) — как было в C#-версии */
    top: 66px;
    left: 256px;
    width: 220px;
    padding: var(--space-6);
    background-color: var(--surface-panel);
    border-color: var(--border-subtle);
    border-width: var(--space-1);
    flex-direction: column;
}

.hud-status-line {
    font-size: var(--size-md);
    margin-bottom: var(--space-1);
    white-space: normal;
}

.hud-skill-container {
    position: absolute;
    bottom: var(--space-5);
    right: var(--space-5);
    padding: var(--space-4);
    flex-direction: column-reverse;
    align-items: flex-end;
}

.hud-skill-row {
    flex-direction: row;
    align-items: flex-start;
    margin-bottom: var(--space-1);
}

.hud-skill-icon {
    flex-direction: row;
    align-items: flex-end;
    margin-right: var(--space-2);
    margin-bottom: var(--space-1);
}

.hud-skill-icon-column {
    position: relative;
    flex-direction: column;
    align-items: center;
    width: var(--space-10);
}

.hud-skill-arrow {
    position: absolute;
    top: -13px;
    left: 0;
    width: var(--space-10);
    font-size: var(--size-xs);
    -unity-font-style: bold;
    color: var(--accent-gold);
    -unity-text-align: middle-center;
}

.hud-skill-bar-bg {
    width: var(--space-9);
    height: 3px;
    background-color: var(--surface-shelf-solid);
    border-radius: var(--radius-sharp);
    margin-top: 1px;
}

.hud-skill-bar-fill {
    height: 3px;
    border-radius: var(--radius-sharp);
    background-color: var(--light-edge);
}

.hud-crystal-row {
    flex-direction: row;
    margin-bottom: 1px;
    align-items: center;
}

.hud-crystal-row .hud-stat {
    margin: 0 var(--space-2);
}

.hud-crystal-dot {
    width: var(--space-7);
    height: var(--space-7);
    margin-right: var(--space-3);
    align-self: center;
}

.hud-crystal-label {
    font-size: var(--size-xs);
    color: var(--text-primary);
}

.hud-skill-icon-image {
    width: var(--space-10);
    height: var(--space-10);
}

.hud-skill-bar-container {
    width: var(--space-3);
    height: var(--space-10);
    margin-left: var(--space-1);
    flex-direction: column;
    justify-content: flex-end;
}

.hud-skill-bar-segment {
    width: var(--space-3);
    height: var(--space-1);
}

.hud-mission-panel {
    position: absolute;
    top: 50px;
    left: 50%;
    translate: -50% 0;
    min-width: 300px;
    padding: var(--space-4);
    background-color: var(--surface-panel);
    border-color: var(--border-subtle);
    border-width: 1px;
    flex-direction: column;
}

.hud-mission-title {
    color: var(--accent-gold);
    margin-bottom: var(--space-2);
}

.hud-mission-desc {
    margin-bottom: var(--space-4);
}

.hud-mission-progress-row {
    flex-direction: row;
    align-items: center;
    margin-bottom: var(--space-2);
}

.hud-mission-progress-label {
    margin-right: var(--space-4);
    min-width: 50px;
}

.hud-mission-progress-bar {
    flex-grow: 1;
    height: var(--space-8);
    background-color: var(--surface-shelf-solid);
    border-radius: var(--radius-sm);
}

.hud-mission-progress-fill {
    height: var(--space-8);
    border-radius: var(--radius-sm);
    background-color: var(--accent-gold);
}

.hud-btn-action {
    position: absolute;
    background-color: var(--surface-shelf-dense);
    border-color: var(--accent-cyan-deep);
    border-width: var(--space-1);
    border-radius: var(--radius-sm);
    font-size: var(--size-sm);
    -unity-font-style: bold;
    color: var(--text-primary);
    -unity-text-align: middle-center;
    padding: 0;
    height: var(--space-11);
}

.hud-btn-action:hover {
    background-color: var(--light-sheen);
}

.hud-toggle-btn {
    position: absolute;
    left: var(--space-5);
    background-color: var(--surface-crisis-dense);
}

.hud-toggle-btn:hover {
    background-color: var(--light-film);
}

.hud-toggle-btn-label {
    font-size: var(--size-sm);
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    flex-grow: 1;
    color: var(--state-danger);
}

.hud-toggle-btn.enabled {
    background-color: var(--surface-slate-dense);
}

.hud-toggle-btn.enabled:hover {
    background-color: var(--light-sheen);
}

.hud-toggle-btn-label.enabled {
    color: var(--state-ok);
}

.hud-toggle-auto-dig { bottom: 281px; }
.hud-toggle-aggression { bottom: 314px; }
.hud-toggle-collision { bottom: 347px; }

.hud-btn-top-row {
    position: absolute;
    top: var(--space-5);
    width: 100px;
    height: var(--space-11);
}

/* Смещения кнопок верхнего ряда (были inline right в C#-версии) */
.hud-btn-top-row--respawn { right: 222px; }
.hud-btn-top-row--buildings { right: 116px; }
.hud-btn-top-row--faq { right: 10px; }

.hud-bonus-button {
    position: absolute;
    left: 256px;
    top: var(--space-5);
    width: 90px;
    height: 50px;
}

.hud-chat-button {
    position: absolute;
    left: var(--space-5);
    bottom: 248px;
    width: 100px;
}

.hud-programmator-button {
    position: absolute;
    left: var(--space-5);
    bottom: 215px;
    width: 100px;
}

/* ═══════════════════════════════════════════════════════════
   Sci-Fi Player HUD Styles
   ═══════════════════════════════════════════════════════════ */

.hud-top-left {
    position: absolute;
    top: var(--space-8);
    left: var(--space-8);
    width: 280px;
}

.hud-title-row {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-4);
}

.hud-nickname {
    -unity-font-definition: var(--face-display);
    color: var(--text-primary);
    font-size: var(--size-base);
    -unity-font-style: bold;
    letter-spacing: 0.5px;
}

.hud-clan-btn {
    height: var(--space-10);
    padding: 0 var(--space-4);
    font-size: var(--size-2xs);
}

.hud-separator {
    height: 1px;
    background-color: var(--border-subtle);
    margin-bottom: var(--space-4);
}

.hud-stat-group {
    margin-bottom: var(--space-4);
}

.hud-stat-header {
    flex-direction: row;
    justify-content: space-between;
    margin-bottom: var(--space-2);
}

.hud-stat-title {
    -unity-font-definition: var(--face-body);
    color: var(--text-secondary);
    font-size: var(--size-xs);
}

.hud-stat-percent {
    -unity-font-definition: var(--face-data);
    color: var(--state-ok);
    font-size: var(--size-xs);
    -unity-font-style: bold;
}

.hud-bar-track {
    height: var(--space-3);
    margin-bottom: var(--space-3);
}

.hud-currency-row {
    flex-direction: row;
    justify-content: space-between;
    margin-top: var(--space-2);
}

.hud-currency-item {
    flex-direction: row;
    align-items: center;
}

.hud-currency-icon {
    font-size: var(--size-base);
    color: var(--accent-cyan);
    margin-right: var(--space-2);
}

.hud-currency-icon--gold {
    color: var(--accent-gold);
}

.hud-currency-value {
    -unity-font-definition: var(--face-data);
    color: var(--text-primary);
    font-size: var(--size-sm);
    -unity-font-style: bold;
}

.hud-currency-value--gold {
    color: var(--accent-gold-hover);
}

/* Корзина */
.hud-basket-panel {
    position: absolute;
    top: 170px;
    left: var(--space-8);
    width: 140px;
    padding: var(--space-4);
}

.hud-basket-grid {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: var(--space-2);
    margin-bottom: var(--space-2);
}

.hud-geology-text {
    -unity-font-definition: var(--face-body);
    color: var(--text-secondary);
    font-size: var(--size-2xs);
}

/* Миссия */
.hud-mission-panel {
    position: absolute;
    top: 50px;
    left: 50%;
    translate: -50% 0;
    width: 380px;
    padding: var(--space-5) var(--space-7);
}

.hud-mission-header {
    flex-direction: row;
    align-items: center;
    margin-bottom: var(--space-2);
}

.hud-mission-title {
    -unity-font-definition: var(--face-display);
    color: var(--text-primary);
    font-size: var(--size-sm);
    -unity-font-style: bold;
    margin-left: var(--space-4);
}

.hud-mission-desc {
    -unity-font-definition: var(--face-body);
    color: var(--text-secondary);
    font-size: var(--size-xs);
    white-space: normal;
    margin-bottom: var(--space-3);
}

.hud-mission-track {
    height: var(--space-2);
    margin-bottom: var(--space-2);
}

#MissionProgressFill {
    width: 0%;
}

.hud-mission-progress-text {
    -unity-font-definition: var(--face-data);
    color: var(--accent-cyan);
    font-size: var(--size-2xs);
    align-self: flex-end;
}

/* Правая колонка действий */
.hud-actions-column {
    position: absolute;
    top: var(--space-8);
    right: var(--space-8);
    width: 160px;
}

.hud-action-btn {
    margin-bottom: var(--space-4);
    width: 100%;
}

.hud-mode-led {
    width: var(--space-3);
    height: var(--space-3);
    border-radius: var(--radius-sm);
    background-color: var(--text-disabled);
    margin-right: var(--space-4);
}

.hud-mode-led--active {
    background-color: var(--accent-cyan-hover);
}


/* ─── Системная миникарта (UI Toolkit) ─── */

.hud-minimap-panel {
    position: absolute;
    bottom: var(--space-8);
    left: var(--space-8);
    padding: var(--space-3);
    background-color: var(--surface-slate-dense);
    border-width: 1px;
    border-color: var(--accent-cyan-fill);
    border-radius: var(--radius-md);
    flex-direction: column;
    align-items: center;
}

.hud-minimap-container {
    width: 160px;
    height: 160px;
    border-width: 1px;
    border-color: var(--light-sheen);
    border-radius: var(--radius-sharp);
    background-color: var(--shadow-solid);
}

.hud-minimap-image {
    width: 100%;
    height: 100%;
}

.hud-minimap-coords {
    font-size: var(--size-xs);
    color: var(--accent-cyan-hover);
    -unity-font-style: bold;
    margin-bottom: var(--space-2);
    -unity-text-align: middle-center;
}

/* ─── Указатель миссии ─── */

.mission-arrow {
    position: absolute;
    width: var(--space-10);
    height: var(--space-10);
    border-top-width: var(--space-2);
    border-right-width: var(--space-2);
    border-bottom-width: 0;
    border-left-width: 0;
    border-top-color: var(--accent-gold);
    border-right-color: var(--accent-gold);
}
