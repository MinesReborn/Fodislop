# Решение по формату текстур: KTX2/BasisU vs PNG — полный документ для тимлида

Статус: решение НЕ принято. Документ содержит всё нужное для принятия: контекст, варианты, цифры, риски, план и критерии приёмки.

## 1. Резюме для быстрого решения

- Проблема: текстуры жрут VRAM (RGBA32, 4 Б/пкс), грузятся inflate-декодером на CPU, вопрос веса на CDN открыт.
- Вариант A (KTX2): VRAM ×4 меньше — факт; вес на CDN — НЕ факт, может проиграть; цена — нативная зависимость + пайплайн + распыление на два режима.
- Вариант B (дожать PNG): −20–40% веса за вечер скриптом, ноль риска, но VRAM не чинит.
- Предлагаемый ход: 1 час эксперимента (раздел 6) → решение по цифрам. Без эксперимента решение не принимать.

## 2. Контекст: как устроено сейчас

- PNG/GIF/WebP лежат файлами, раздаются с CDN/сервера по ETag/SHA-манифесту, кешируются на диске как байты (`PersistentAssetCache`, версионный манифест).
- Рантайм: `DecodeTexture` → inflate → **RGBA32** в VRAM (4 Б/пкс), мипов нет, sRGB/Point/Clamp принудительно (`RuntimeTextureFactory`).
- Анимации: GIF/WebP (кадры/UV-скролл). Png-текстуры: ~326 файлов, піксель-арт — резкие края, плоские заливки (идеальный случай для PNG).
- Известные дефекты арта (см. TODO, раздел графики): чекбокс 4 размера на 4 состояния, `Programmator` 31 размер иконок, `Pack` не кратны 32, занятость холста 0.05–0.61. Выгодны любому варианту.

## 3. Цель и жёсткие требования

1. Ассеты должны стать компактнее (вес на CDN — числом вниз, см. п.7: бюджет нужен от тебя).
2. VRAM типовой сцены — вниз (сейчас 4 Б/пкс на всём).
3. Холодный лоад — не дольше текущего.
4. Ноль регресса арта глазами (арт попиксельный, lossy-артефакты на краях недопустимы).
5. Откат возможен на каждом этапе.

## 4. Вариант A: KTX2 (Basis Universal)

Один `.ktx2` на все платформы. На клиенте транскод в нативный GPU-блок: ASTC (Apple), BC7/BC3 (Windows), ETC2 (Android). Два режима внутри: ETC1S (минимум веса, lossy) и UASTC (качество, тяжелее). Анимации остаются на GIF/WebP — KTX2 статический, их не покрывает.

### Плюсы

- VRAM ×4 меньше (ASTC 4×4 = 1 Б/пкс) — факт спецификаций, без эксперимента. Тот же выигрыш на шине текстурных семплов (fill-rate террейна).
- Один файл на все GPU вместо платформенных пересборок.
- Транскод ожидаемо быстрее inflate — холодные зоны грузятся короче (подтвердить замером из п.6).
- Протокол/кеш/манифест не меняются: те же байты по тем же хешам, серверный код не трогаем.

### Минусы

- Вес на CDN не гарантирован: ETC1S на резких краях даёт звон и может весить не меньше PNG; UASTC часто тяжелее исходного PNG. Требование «компактнее» может не сойтись вообще.
- Два режима = распыление: матрица «семейство → режим», разные пайпы проверки, разные баги транскода на разных GPU.
- Нативная зависимость BasisU под macOS ARM64 + Windows x64 (+ мобилки потом): сборка, подпись, тесты только в плеер-билдах (в Editor всё врёт).
- Пайплайн: конвертер (`basisu`/`toktx`) локально или в CI + версионирование конвертера + сосуществование `.ktx2`/`.png` с fallback + снос PNG.
- Потеря качества — риск именно на нашем арте (края, плоские заливки, дithering зерна).

## 5. Вариант B: дожать PNG (план Б, независимый)

`oxipng -o6`, снос метаданных, индексация палитры где возможно, плюс арт-гигиена из TODO (холсты/размеры). Тот же путь загрузки, ноль кода.

- Плюсы: ноль риска рантайма; типово −20–40% веса без потерь; вечер скриптом; откат = удалить файлы.
- Минусы: VRAM остаётся 4 Б/пкс; CPU-лоад не ускоряется; это потолок без смены формата.

## 6. Сравнение и эксперимент, который решает всё

| Критерий | PNG сейчас | PNG дожат (B) | KTX2 (A) |
|---|---|---|---|
| Вес CDN | 100% | −20–40% (прогноз) | неизвестно — мерить (п.6) |
| VRAM | 4 Б/пкс | 4 Б/пкс | 1 Б/пкс (факт) |
| Скорость лоада | baseline | та же | ожидаемо быстрее (мерить) |
| Качество | без потерь | без потерь | lossy-риск (смотреть) |
| Код | 0 | 0 | транскодер + путь + fallback |
| Пайплайн | нет | скрипт | конвертер + CI + матрица + манифест |
| Натив | нет | нет | BasisU × платформы |
| Откат | — | trivial | сложный |

Эксперимент (1 час, ноль инфраструктуры, решает за/против):
```bash
toktx --encode etc1s out-etc1s.ktx2 in.png
toktx --encode uastc  out-uastc.ktx2  in.png
```
20 файлов из `Cells`, `Items`, `Skills`, `Crystals`, `Pack`, `Programmator`: байты до/после + развернуть назад и посмотреть края глазами.
- UASTC ≥ PNG и ETC1S мылит → KTX2 отклонить, делать план Б.
- Цифры сошлись → матрица семейство→режим → транскодер → выкладка с fallback.

## 7. Что нужно от тимлида (решения)

1. Целевой бюджет веса текстур на CDN — числом. Без числа «компактнее» непроверяемо.
2. Матрица «семейство → режим (ETC1S/UASTC/PNG)» по итогам эксперимента (п.6) — или поручить её составление с прогнозом байт до/после.
3. Кто смотрит арт после транскода и какой порог отличий (на глаз + метрика).
4. Конвертер: локальный скрипт vs CI-степ; версионирование конвертера; кто держит.
5. Схема сосуществования `.ktx2`/`.png` (fallback), порядок выпиливания PNG, ключи манифеста.
6. Порядок работ: транскодер клиента vs выкладка сервера — что первым, точки отката.

## 8. План работ (после решений из п.7)

1. Эксперимент п.6 → зафиксировать цифры в этот документ.
2. Арт-гигиена из TODO (независима, выгодна обоим вариантам).
3. Транскодер клиента (BasisU, fallback на PNG, тесты в плеер-билдах всех платформ).
4. Конвертация + выкладка `.ktx2` рядом с PNG, ключи манифеста.
5. Замеры: вес CDN, холодный лоад, VRAM типовой сцены, арт глазами.
6. Снос PNG (только после приёмки).

## 9. Критерии приёмки

- Суммарный вес текстур на CDN меньше текущего на согласованный бюджет (п.7.1).
- Холодный лоад не дольше текущего (замер, не ощущение).
- VRAM типовой сцены меньше (ASTC/BC лежат сжатыми — проверить профайлером памяти).
- Ни одного регресса арта глазами (список просмотренных семейств + кто смотрел).
- Fallback на PNG работает при отсутствии `.ktx2` (убить файл на CDN в тесте, игра не падает).
- Откат каждого этапа — одной операцией (какой именно — в плане этапа).

## 10. Риски

- Главный: вес не сойдётся (п.4, минусы) — страхуется экспериментом ДО любых вложений.
- Транскод-баги на конкретных GPU — страхуется матрицей тестовых устройств и fallback.
- Расползание: два формата + два пайпа + GIF/WebP отдельно — держать матрицу из п.7.2 единственным источником, не плодить исключения.
- Потеря качества, заметная только на части сцен, — смотреть не 20 файлов, а типовые сцены целиком (лава, кристаллы, постройки, персонаж).
