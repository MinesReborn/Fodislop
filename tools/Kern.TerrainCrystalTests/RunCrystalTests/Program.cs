namespace Kern.TerrainCrystalTests;

internal static class Program
{
    public static int Main()
    {
        Console.Error.WriteLine("Crystal CPU regression source is not present; no checks were run.");
        return 0;
    }
}
